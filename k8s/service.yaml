apiVersion: v1
kind: Service
metadata:
  name: express-4565-service
  namespace: express-apps
spec:
  type: ClusterIP
  ports:
    - port: 4565
      targetPort: 4565
  selector:
    app: express-4565
